// swap the comments to see the difference in the signatures

import { addNumbers, doubleNum } from './undefinedmath';
// import { addNumbers, doubleNum } from './definedmath';

{

    console.log(addNumbers(1, 2, 3));
    console.log(doubleNum(2));
}
